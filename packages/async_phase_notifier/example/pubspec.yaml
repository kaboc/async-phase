name: async_phase_notifier_example
description: "An example app of AsyncPhaseNotifier."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2
  flutter: ^3.27.4

dependencies:
  flutter:
    sdk: flutter

  async_phase_notifier:
    path: ..

  grab: ^1.0.2
  http: ^1.6.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
