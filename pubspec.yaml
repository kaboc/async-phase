name: async_phase_notifier
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/kaboc/async-phase-notifier

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.7.0

dev_dependencies:
  flutter_lints: ^2.0.1
